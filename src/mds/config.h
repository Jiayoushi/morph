#ifndef MORPH_MDS_CONFIG_H
#define MORPH_MDS_CONFIG_H

namespace morph {

namespace config {

// Swap to a new log file when this total size of batch sizes are written.
static const int MAX_LOG_FILE_SIZE = 1000;

} // namespace config

} // namespace morph

#endif